spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  
  flyway:
    enabled: false
  
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://${DB_HOST:caboose.proxy.rlwy.net}:${DB_PORT:44379}/${DB_NAME:railway}}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:HcPmFIHVFYAFihKKPBSNOPieOMuAcwwj}

logging:
  level:
    com.seaandtea: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
