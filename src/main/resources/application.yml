spring:
  application:
    name: seaandtea-backend
  
  profiles:
    active: dev
  
  # Common configuration shared across all profiles
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Common JWT Configuration
jwt:
  expiration: 86400000 # 24 hours

# Common Stripe Configuration
stripe:
  webhook-secret: ${STRIPE_WEBHOOK_SECRET}

# Common Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

# Common Server Configuration
server:
  servlet:
    context-path: /api/v1

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    redis:
      enabled: false
    mail:
      enabled: false

# Common Logging Configuration
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  
  # Common logging levels
  level:
    root: INFO
    # Enable basic HTTP logging for all profiles
    org.springframework.web: INFO
    org.springframework.web.servlet: INFO
    # Enable comprehensive HTTP logging for development
    com.seaandtea.interceptor: INFO
    com.seaandtea.filter: INFO
    com.seaandtea.util: INFO
